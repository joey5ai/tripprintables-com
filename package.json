{
  "name": "tripprintables-com",
  "version": "1.0.0",
  "private": true,
  "description": "tripprintables.com — free travel planning templates",
  "scripts": {
    "build": "eleventy",
    "serve": "eleventy --serve",
    "audit": "node scripts/audit.js",
    "prepare": "git config core.hooksPath .githooks || true"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
